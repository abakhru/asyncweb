user  nginx;

events {
    worker_connections   1000;
}
http {
        server {
              listen 80;
              client_max_body_size 30M;
              location / {
                    proxy_pass http://web:8080;
              }
        }
}
